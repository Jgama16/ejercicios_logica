// solucion de ejercicio 2
const numeoT = [3, 1, 3, 3, 2, 4, 5, 6, 5, 4];
let arr = numeoT.map (x => numeoT.map(el => Math.floor(Marth.random() * 100)));
console.log("numero de telefono");
console.table(arr);

